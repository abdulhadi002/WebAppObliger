export default function Streaks() {
    const streaks = 10;

    return (
        <main>
            <h1>This is your streaks: {streaks}</h1>
            <p>This is the secound page</p>
        </main>
    );
}